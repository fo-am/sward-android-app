<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/colorBackground</item>
        <item name="android:textColor">@color/colorText</item>
        <item name="buttonStyle">@style/SwardButton</item>
        <item name="android:buttonStyle">@style/SwardButton</item>
        <item name="android:buttonStyleToggle">@style/SwardToggleButton</item>
        <item name="android:fontFamily" tools:targetApi="jelly_bean">@font/sen_regular</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="SwardThemeOverlay_Toolbar" parent="ThemeOverlay.MaterialComponents.Toolbar.Primary">
        <!-- color used by navigation icon and overflow icon -->
        <item name="colorOnPrimary">@color/colorText</item>
        <item name="titleTextColor">@color/colorText</item>
        <item name="android:background">@color/colorBackground</item>
       <!-- <item name="android:popupTheme">@style/SwardPopupMenuStyle</item> -->
        <!-- <item name="android:src"></item> -->
    </style>

    <style name="SwardButton" parent="Widget.AppCompat.Button">
        <item name="android:textAllCaps">false</item>
        <item name="android:background">@drawable/button</item>
        <item name="android:textSize">10pt</item>
    </style>

    <style name="SwardToggleButton"  parent="Widget.AppCompat.Button">
        <item name="android:textAllCaps">false</item>
        <item name="android:background">@drawable/button_toggle</item>
        <item name="android:textSize">10pt</item>
    </style>

    <style name="SwardImageButton" parent="SwardButton">
        <item name="android:drawableRight">@drawable/test_button</item>
        <item name="android:gravity">left|center_vertical</item>
        <item name="android:paddingLeft">30dp</item>
        <item name="android:paddingRight">5dp</item>
    </style>

    <style name="SwardPopupMenuStyle" parent="Theme.AppCompat.Light">
        <item name="android:background">@color/colorMenuBackground</item>
        <item name="android:textColor">@color/colorMenuText</item>
    </style>
</resources>
